package com.wabridge.app

/**
 * Bump BUILD_TAG every time you ship a meaningful change and want to be
 * able to visually confirm (via the on-screen event log or the app
 * title) that the device is actually running the new build and not a
 * stale install.
 */
object BuildInfo {
    const val BUILD_TAG = "FIX40-experimental-phone-learn-2026-08-14"
}
